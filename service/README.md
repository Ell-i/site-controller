CoAP server as service
======================

To install on linux with username "ubuntu"

    sudo cp coapserver.conf /etc/init/.

To handle the service

    sudo service coapserver <start/restart/stop/status>
